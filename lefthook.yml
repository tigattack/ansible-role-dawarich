---

pre-commit:
  jobs:
    - name: Patch Dawarich version in README
      glob: "min_dawarich_version.txt"
      run: .github/patch_version.sh
      skip:
        - merge
        - rebase

remotes:
  - git_url: git@github.com:tigattack/repo_utils.git
    # refetch: true
    configs:
      - lefthook_ansible_role.yml
